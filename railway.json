{
  "build": {
    "nixpacksPlan": {
      "providers": ["php", "composer"],
      "overrides": {
        "php": {
          "version": "8.2"
        }
      }
    }
  },
  "deploy": {
    "startCommand": "php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
